#include <stdio.h>
#include <string.h>

/**
 * _strlen - function that returns the length of a string.
 * @s: a pointer of char
 *
 * Return: return the length of the string.
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
